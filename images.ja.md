# 画像

## 概要

ONEPIECE Framework では、画像も framework の流れの中で扱うことができます。

つまり、画像リクエストは単なる静的ファイル配信に限定されず、他の framework 管理レスポンスと同じようにアプリケーション側で制御できます。

## 仕組み

どの URL で `app.php` を実行するかは、Webサーバーの設定に従います。

Apache の場合は、その挙動は `.htaccess` に記述されています。

そのため、画像の扱いも `.htaccess` の routing ルールに影響されます。

現在の Apache 設定では、

- 要求された画像ファイルが物理的に存在しない場合は `app.php` に到達する
- 要求された画像ファイルが物理的に存在する場合は Apache が静的配信する

存在しない画像リクエストが `app.php` に到達した場合、その後は次の流れになります。

1. `app.php` が framework を起動する
2. endpoint の `index.php` が選ばれて実行される
3. アプリケーション側で画像レスポンスを動的に生成または変更できる

## できること

この設計により、次のことが可能になります。

- 画像レスポンスを framework の制御下に置く
- 画像を動的に生成する
- 状況に応じて画像の出力内容を変える
- 画像を返す前にアプリケーション側の条件判断を行う
- 物理ファイルと一致しない画像 URL を扱う
- 必要に応じて、通常の既存画像は静的配信のまま扱う

## アクセス制御の用途

この仕組みは、常に同じ画像をすべての相手に見せたくない場合に有効です。

たとえば、アプリケーション側で条件を見て、

- 画像を返すかどうか
- どの画像を返すか

を判断できます。

これにより、次のような相手に対して、単純に同じ画像を公開し続けることを避けられます。

- 閲覧を許可していない相手
- 不特定多数の相手
- URL を直接入力してアクセスする相手

## テキストベース画像

SVG のような、実体がテキストである画像形式は特にこの方式と相性が良いです。

SVG はテキストなので、PHP で動的に生成したり、修正したりしてからレスポンスとして返せます。

## まとめ

ONEPIECE Framework では、画像は単なる静的 asset としてだけ扱われるわけではありません。

Webサーバーの routing 設定に応じて、framework 管理下のレスポンスとして扱うことができるため、動的生成、条件分岐、アプリケーション制御付きの配信が可能になります。
